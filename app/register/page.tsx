'use client';import { useState } from 'react';import Link from 'next/link';
export default function Register(){const[f,setF]=useState({email:'',password:''});const[err,setErr]=useState('');const submit=async(e:any)=>{e.preventDefault();const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});if(r.ok) location.href='/'; else setErr('Бүртгэл амжилтгүй')};return <main className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="card w-full max-w-md space-y-4"><h1 className="text-3xl font-black">✨ Бүртгүүлэх</h1><input className="input" placeholder="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input className="input" placeholder="password" type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/>{err&&<p className="text-rose-500">{err}</p>}<button className="btn w-full bg-indigo-600 text-white">Бүртгүүлэх</button><Link href="/login" className="block text-center text-indigo-600">Нэвтрэх</Link></form></main>}
